#include <stdio.h>

void f1(int numbers[5]);

int main(void) {

    int numbers[5]; 

    while (user inputting)
        // put numebr in array

    int result = f1(size, numbers);

    printf("%d\n", a);

    return 0;
}

void f1(int size, int numbers[5]) {

    int i = 0;

    while (i < size) {
        // code
    }

}